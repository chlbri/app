/**
 * Immutable empty object default constant.
 */
export const EMPTY_OBJECT = {} as const;

/**
 * Constant object mapping guard evaluation types ('guard', 'and', 'or').
 */
export const GUARD_TYPE = { guard: 'guard', and: 'and', or: 'or' } as const;

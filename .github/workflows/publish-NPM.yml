name: Publish
on:
  push:
    branches:
      - beta
      - canary
      - main
      - master

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout pnpm repo
        uses: chlbri/node-github-actions/.github/actions/pnpmi@main

      - name: Build
        run: pnpm run build

      - name: Upload workspace
        uses: actions/upload-artifact@v7
        with:
          name: built-workspace
          path: |
            .
            !node_modules
            !.git
          retention-days: 1

  app:
    needs: [build]
    uses: chlbri/node-github-actions/.github/workflows/publish.yml@main
    secrets: inherit
    with:
      type:
        ${{ github.ref_name == 'beta' && 'beta' || github.ref_name ==
        'canary' && 'canary' || 'latest' }}

      package: './packages/app'
      build: false

  vitest:
    needs: [build]
    uses: chlbri/node-github-actions/.github/workflows/publish.yml@main
    secrets: inherit
    with:
      type:
        ${{ github.ref_name == 'beta' && 'beta' || github.ref_name ==
        'canary' && 'canary' || 'latest' }}

      package: './packages/vitest'
      build: false

  solidjs:
    needs: [build]
    uses: chlbri/node-github-actions/.github/workflows/publish.yml@main
    secrets: inherit
    with:
      type:
        ${{ github.ref_name == 'beta' && 'beta' || github.ref_name ==
        'canary' && 'canary' || 'latest' }}

      package: './packages/solidjs'
      build: false

  reactjs:
    needs: [build]
    uses: chlbri/node-github-actions/.github/workflows/publish.yml@main
    secrets: inherit
    with:
      type:
        ${{ github.ref_name == 'beta' && 'beta' || github.ref_name ==
        'canary' && 'canary' || 'latest' }}

      package: './packages/reactjs'
      build: false

  cli:
    needs: [build]
    uses: chlbri/node-github-actions/.github/workflows/publish.yml@main
    secrets: inherit
    with:
      type:
        ${{ github.ref_name == 'beta' && 'beta' || github.ref_name ==
        'canary' && 'canary' || 'latest' }}

      package: './packages/cli'
      build: false

  valibot:
    needs: [build]
    uses: chlbri/node-github-actions/.github/workflows/publish.yml@main
    secrets: inherit
    with:
      type:
        ${{ github.ref_name == 'beta' && 'beta' || github.ref_name ==
        'canary' && 'canary' || 'latest' }}

      package: './packages/valibot'
      build: false

  cleanup:
    name: Cleanup Artifacts
    runs-on: ubuntu-latest
    needs: [build, app, vitest, solidjs, reactjs, cli, valibot]
    if: always()
    permissions:
      actions: write
      contents: read

    steps:
      - name: Delete artifacts
        uses: actions/github-script@v8
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            try {
              const artifacts = await github.rest.actions.listWorkflowRunArtifacts({
                owner: context.repo.owner,
                repo: context.repo.repo,
                run_id: context.runId,
              });

              for (const artifact of artifacts.data.artifacts) {
                if (artifact.name === 'built-workspace') {
                  console.log(`Deleting artifact: ${artifact.name} (ID: ${artifact.id})`);
                  await github.rest.actions.deleteArtifact({
                    owner: context.repo.owner,
                    repo: context.repo.repo,
                    artifact_id: artifact.id,
                  });
                  console.log(`Successfully deleted artifact: ${artifact.name}`);
                }
              }
            } catch (error) {
              console.log(`Error during cleanup: ${error.message}`);
            }
